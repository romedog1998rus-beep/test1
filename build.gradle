apply plugin: 'com.android.application'

android {
    namespace "com.forge.signal"
    compileSdkVersion 34

    defaultConfig {
        applicationId "com.forge.signal"
        minSdkVersion 26
        targetSdkVersion 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

dependencies {
}
